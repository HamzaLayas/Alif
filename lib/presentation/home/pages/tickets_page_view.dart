import 'package:flutter/material.dart';

class TicketsPageView extends StatelessWidget {
  const TicketsPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}